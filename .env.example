# ====================
# Application Settings
# ====================
APP_NAME=vibe-web-starter
APP_VERSION=0.1.0
DEBUG=True
ENVIRONMENT=development

# ====================
# API Settings
# ====================
API_V1_PREFIX=/api/v1
ALLOWED_ORIGINS=["http://localhost:3000","http://localhost:8000"]

# ====================
# Database Settings
# ====================
# 사용할 데이터베이스 타입 선택 (postgresql 또는 mssql)
DATABASE_TYPE=postgresql

# ✅ 옵션 1: DATABASE_URL 직접 설정 (권장)
# PostgreSQL 예시:
DATABASE_URL=postgresql+asyncpg://postgres.YOUR_PROJECT_ID:YOUR_PASSWORD@aws-1-ap-northeast-2.pooler.supabase.com:5432/postgres

# MSSQL 예시:
#DATABASE_URL=mssql+aioodbc://sa:YourPassword@localhost:1433/master?driver=ODBC+Driver+17+for+SQL+Server&timeout=30&Encrypt=no&TrustServerCertificate=yes

# ✅ 옵션 2: 개별 변수로 설정 (DATABASE_URL 사용 시 아래 변수들은 무시됨)

# PostgreSQL 개별 설정
#POSTGRES_HOST=localhost
#POSTGRES_PORT=5432
#POSTGRES_USER=postgres
#POSTGRES_PASSWORD=postgres
#POSTGRES_DB=vibe-web-starter

# ====================
# DB Engine Options
# ====================
DB_ECHO=False
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10

# ====================
# Security Settings
# ====================
SECRET_KEY=your-secret-key-here-change-in-production-use-openssl-rand-hex-32
ACCESS_TOKEN_EXPIRE_MINUTES=30

# ====================
# Logging Settings
# ====================
LOG_LEVEL=INFO

# ====================
# MSSQL Settings
# ====================
# MSSQL 개별 설정 (DATABASE_TYPE=mssql이고 DATABASE_URL이 없을 때 사용)
# 퍼블리싱 시 MSSQL_HOST만 서버 IP(예: 172.17.1.36)로 변경하면 즉시 작동합니다
MSSQL_DRIVER=ODBC Driver 17 for SQL Server
MSSQL_HOST=localhost
MSSQL_PORT=1433
MSSQL_DATABASE=master
MSSQL_USER=sa
MSSQL_PASSWORD=YourPassword
MSSQL_TIMEOUT=30
MSSQL_ENCRYPT=False
MSSQL_TRUST_SERVER_CERTIFICATE=True

# 참고: MSSQL 사용 전 ODBC Driver 17 for SQL Server 설치 필요
# - Windows: https://docs.microsoft.com/en-us/sql/connect/odbc/download-odbc-driver-for-sql-server
# - Linux (Ubuntu/Debian): https://docs.microsoft.com/en-us/sql/connect/odbc/linux-mac/installing-the-microsoft-odbc-driver-for-sql-server
# - macOS: brew install msodbcsql17

# ====================
# Claude Code MCP 설정
# ====================
# Claude Code에서 MSSQL MCP 서버를 사용하려면:
# 1. 프로젝트 루트에 .mcp.json 파일을 생성하세요 (아래 예시 참고)
# 2. .mcp.json은 .gitignore에 포함되어 있으므로 Git에 올라가지 않습니다
#
# .mcp.json 예시:
# {
#   "mcpServers": {
#     "mssql": {
#       "command": "npx",
#       "args": ["-y", "@knight0zh/mssql-mcp-server"],
#       "env": {
#         "MSSQL_HOST": "localhost",
#         "MSSQL_PORT": "1433",
#         "MSSQL_DATABASE": "master",
#         "MSSQL_USER": "sa",
#         "MSSQL_PASSWORD": "YourPassword",
#         "MSSQL_ENCRYPT": "false",
#         "MSSQL_TRUST_SERVER_CERTIFICATE": "true"
#       }
#     }
#   }
# }

# ====================
# Domain Plugin Settings
# ====================
# 도메인별 설정을 추가할 수 있습니다
# ENABLE_SAMPLE_DOMAIN=True
